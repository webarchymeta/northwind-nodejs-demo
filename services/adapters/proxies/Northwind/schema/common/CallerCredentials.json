/// <summary>
/// A class holding user credentials for authentication purposes.
/// </summary>
{
    "name": "CallerCredentials",
    "type": "object",
    "properties": {
        "UserName_": {
            "type": "string",
            "required": true,
            "description": "The user-name to identify the client."
        },
        "Password_": {
            "type": "string",
            "required": true,
            "description": "The password used to sign-in the service, if needed."
        }
    }
}